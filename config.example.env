# Google Gemini API key
# Get your free API key at: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_api_key_here

# Optional: Input folder path (default: images_to_process)
# INPUT_FOLDER=images_to_process

# Optional: Output folder path (default: output)
# OUTPUT_FOLDER=output

# Optional: Maximum API calls per minute (default: 14 to stay under free tier 15 RPM limit)
# MAX_RPM=14

# Optional: Delay between batches in seconds (default: 5)
# BATCH_DELAY=5

# Optional: Number of images per batch (default: 10)
# BATCH_SIZE=10

# Optional: Models to use in order (comma-separated). The app automatically
# switches to the next model when a rate limit (429) is hit.
# Default order: gemini-2.5-flash-lite, gemini-3-flash, gemini-2.5-flash
# GEMINI_MODELS=gemini-2.5-flash-lite,gemini-3-flash,gemini-2.5-flash

# Optional: Single Gemini model to use (ignored when GEMINI_MODELS is set)
# GEMINI_MODEL=gemini-1.5-flash

# Optional: Minimum similarity score (0.0–1.0) to automatically merge a new item
# into an existing catalog entry across runs (default: 0.80)
# DUPLICATE_MERGE_THRESHOLD=0.80

# Optional: Filename for the persistent item catalog stored under the output folder
# (default: item_catalog.json)
# ITEM_CATALOG_FILENAME=item_catalog.json
